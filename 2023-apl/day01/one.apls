file ← ⎕NGET input.txt
hi ← 2×3

⍝ first attempt
file ← ⊃⎕NGET '/Users/daniel/RemoteGit/advent-of-code/2023-apl/day01/input.txt'
splitByNL ← (~file ∊ (⎕UCS 10))⊆file
onlyNums ← {(⍵∊⎕D) / ⍵}¨splitByNL
firstAndLast ← {(⊃⍵) ((≢⍵)⊃⍵)}¨onlyNums
ans ← +⌿⍎¨firstAndLast
